This fixture represents a post-spec, pre-plan project state.

The autoresearch brainstorming stage has completed and the spec has been approved.
No adaptation plan has been written yet — that is the job of the autoresearch-planning skill.
The `autoresearch/state.yaml` reflects this: `active_spec_path` is set, `active_plan_path` is null.
