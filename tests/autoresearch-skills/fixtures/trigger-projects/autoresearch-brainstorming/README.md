This fixture represents the pre-spec state of an autoresearch project: no spec has been written yet, no profile has been frozen, and no planning or bootstrapping has begun.

The `autoresearch/state.yaml` snapshot reflects the moment just before `autoresearch-brainstorming` runs — `profile_status` is null and `active_spec_path` is null.

This trigger fires when a small research repo needs diagnosis and profile scoping before any repo changes or experiment runs begin.
