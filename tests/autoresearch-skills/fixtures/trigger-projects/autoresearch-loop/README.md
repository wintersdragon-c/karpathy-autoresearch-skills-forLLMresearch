# autoresearch-loop trigger fixture

Post-bootstrap, pre-run loop state. Profile and baseline are validated; the loop has not yet started.

Includes one admitted run example (the baseline run) in results.tsv, ledger.jsonl, and runs/ to demonstrate the expected artifact shape after the first keep outcome.
